# MiniMax MCP Skill

Minimal pointer for terminal/extension use. See `SKILL.md` for full usage.

Quick start:
- `./scripts/web-search.sh "query"`
- `./scripts/analyze-image.sh "prompt" "image.png"`

MCP server:
- `uvx minimax-coding-plan-mcp -y`
